services:
  mcp-server:
    build: .
    container_name: virtual-tables
    restart: always
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - MCP_NAME=Virtual Tables
      - TRANSPORT=streamable-http
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/mcp"]
      interval: 30s
      timeout: 10s
      retries: 3
